<view class="container">
  <view class="tabs">
    <van-tabs active="{{ active }}" bind:change="onChange">
      <van-tab wx:for="{{menuList}}" wx:key="index" title="{{item}}"></van-tab>
    </van-tabs>
  </view>
  <view class="content">
    <order-item></order-item>
  </view>
</view>