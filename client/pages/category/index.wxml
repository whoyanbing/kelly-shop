<view class="container">
  <van-grid>
    <van-grid-item url="/pages/productList/index" icon="{{item.icon?item.icon:'photo-o'}}" text="{{item.name}}" wx:for="{{ categoryList }}" wx:key="index" />
  </van-grid>
</view>